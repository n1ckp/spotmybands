import {} from '../actions'

const getInitialState = () => {
  return {

  }
}

export default function performances(state = getInitialState(), action) {
  let updatedState = Object.assign({}, state)

  return updatedState
}
